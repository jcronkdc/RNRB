version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: songforge
      POSTGRES_PASSWORD: songforge
      POSTGRES_DB: songforge_dev
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

