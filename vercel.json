{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/db && rm -rf node_modules/.prisma && prisma generate --no-engine && cd ../.. && pnpm build",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}
