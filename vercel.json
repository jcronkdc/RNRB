{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/db && prisma generate && cd ../.. && pnpm build",
  "framework": null,
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}
