{
  "buildCommand": "cd apps/web && pnpm build",
  "installCommand": "corepack enable && pnpm install --prod=false",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "env": {
    "NODE_ENV": "development"
  }
}
