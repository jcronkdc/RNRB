{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/db && pnpm prisma:generate && pnpm build && cd ../.. && pnpm build",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}
